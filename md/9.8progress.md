# 9.8 进度日志(本日首次会话自动创建于 10:38)

> 随做随记;会话结束前补全"当前状态与下一步"。记法沿用 9.4progress.md。

## 今日事项

### v2.1 合并式管线收尾(承接 9.7 §16)

1. **流式训练 OOM 三连查(终因找到)**:`CurveShardDataset.__iter__` 切批逻辑错——每进 2048 行只切一个 512 批,**剩余全留在 carry 里滚雪球**(每轮净增 1536),收尾时把 34 万行当一批吐出,注意力矩阵单次要 20GiB。判据:probe 探针显示 batch224 形状 (342054,40) 一锤定音。修复:`while n_carry >= batch_size` 循环切净。修后 stream 模式全链 exit 0。
2. **stream 模式收敛性验证通过**:400k 行/epoch × 2 epoch,val_rec 0.0062 ≈ npz 路径同语料 0.0068,CLS 信息比 ×13.9~14.6,acc 与 npz 同水平——**流式与物化语料训练等价**。前置修的两个小 bug:DataLoader 默认转换会把 numpy 字典 tensor 化(collate_fn=identity 修复);stream_batches 产出顺序 (s,v,l,y,m) 与解包 (s,v,l,m,y) 错位。
3. **用户否决打包 python 环境**:minipy3 无 pandas/numpy(实测 tar 清单,只有 flask/redis 类杂项),mapInPandas 在 yarn 会挂;用户要求"用 pyspark 适配"→ **build_curves_spark.py 重写为纯 Spark SQL**:窗口函数(pre_cum 比例前缀和/样本级聚合)+ collect_list(struct) sort_array + transform()/aggregate() 高阶函数生成 speed/valid 数组,**executor 端零 python**,minipy3 直接可用。yarn 资源较 ingest 调大(executor 8g+overhead 4096,driver 12g——窗口+groupBy 链条 shuffle 更重)。
4. **`scripts/submit_curves_yarn.sh` 落地**(三档 local/dry/yarn,套路同 ingest 版);dry 档验证路由正确。待 SQL 版本地等价验证过 → 首提 day17。
5. 待办:SQL 版 curves 本地跑+逐键等价(vs profiles npz,判据同 9.7 §16:键/标签逐位、speeds 容差 3.8e-6)→ yarn 首跑 → 7 天曲线分片 → stream 正式预训练(入口脚本现成,语料路径换 curves 分片目录)。
6. **纯 SQL 版本地验证完成(61 stage,~40min,3 文件)**:行数 3,653,953 与参照 npz 精确一致;键集合/lengths/valid/n_bins 完全一致;y 在 f32 对齐后逐位一致(npz 存 f32,SQL 版保留 double,直接比有 6e-5 假差异——比较时必须对齐精度,踩坑记档);v/td 容差 1e-4 内全过;speeds 与 pandas 版同一差异模式(26.5% 位置 ≤3.8e-6,T_diff f32 舍入,SQL 版更准)。**判决:v2.1 合并管线(纯 SQL)语义等价,executor 零 python,可上集群**。另:pandas 的 `df.sub` 是减法方法名,取列必须用 `df["sub"]`。
7. **yarn 首跑 day17 已提交**(11:0x):`tl_curves_20260817` = application_1782979140233_46705854,SUBMITTED 排队中;输出 `hdfs://.../curves_spark/day20260817`(curves/ + samples.parquet + link_window.parquet)。跑通后逐天提 18~23。本地验证使命完成,后续大数据全在集群。

## 1. stream 链路审查 + CUDA OOM 根因与修复(晚会话)

**用户新增 v2.1 stream 链路**:`build_curves_spark.py`(一个 Spark 作业:ingest 语义 + mapInPandas 跑 per-sample curve 逻辑,直出 curves/ parquet shards + samples/link_window + edges 元数据)→ `pretrain_stream.py`(IterableDataset 流式消费)→ `train_pretrain.py --config pretrain_stream.yaml`(mode=stream 分支)。smoke3 跨链路验证:curve 数 3,653,953 与 profiles 链完全一致 ✅。鲁班 k8s-job(npz 路径)8 epoch 跑通(app k8s-job-2mholm,sacc_hour=1.0 系单小时语料 artifact)。

**stream 训练 OOM 根因**(smoke2/smoke3 两次确定性复现,数字一致:已分配 33.85GiB + 请求 20.06GiB):
- 关键算术:20.06GiB = 73,344²×4B **精确相等**,73k ≈ val 行数(2% shards)→ eval 快速路径里产生了 [N_val×N_val] fp32 方阵
- 落点 `torch._transformer_encoder_layer_fwd`(nn.TransformerEncoder 的 MHA fused fastpath,仅 eval+no_grad 走);实测该路径单次前向 workspace 比关闭大 8 倍(1.095 vs 0.135GiB)
- 孤立实验三连:train-only 平/val-only 平/短train+val 活 → 需完整 train 阶段在前才触发(具体保持机制未完全定位,但方阵尺度∝val_rows²)
- **不能裸搬 A100-80G**:54GB 峰值本卡放得下,但方阵 ∝ val²;7 天语料 100 万行 val → 1M²×4B=4TB,任何卡都死

**修复**(已落地 train_pretrain.py):①`torch.backends.mha.set_fastpath_enabled(False)`(__main__,附测量注释);②`_metrics` 开头 `torch.cuda.empty_cache()`(训练段碎片保险)。**验证**:原场景(400k 训练行+全量val)从必炸 → 稳过,GPU 峰值 1.080GiB(50× 余量),val_rec 0.0064/cls0 x13.51/7.12min。上集群前提已满足。

**审查遗留 P1(未修,下次)**:①stream 的 train/val 按 shard 切分,但 curves 写出是 round-robin repartition → 同一 sample 的多条 curve 可能跨 shard → **sample 级防泄漏保证被破坏**(建议读侧按 hash(sample_id) 过滤,或写出改按 sample_id 分区);②build_curves_spark `batch_fn` 的 pending 边界分支 `pending.append(chunk)` 未先 clear → 跨 ≥3 个 chunk 的 sample 会丢中间 chunk 行(当前 chunk=10000 行/sample~几十,实际不可达,一行修复);③`workers` 配置读了没用(DataLoader 硬编码 num_workers=0);④build_curves_spark 的 dedup persist(DISK_ONLY)未按 local master 跳过(同 9.7 的配额坑)。

**P1 处置(11:4x,午间会话)**:①已修——`build_curves_spark.py` curves 写出改 `repartition(n, sample_id)`(hash 分片,一样本一分片,shard 级 val 切分恢复样本级防泄漏);**day17 在跑的是修复前版本(11:22 提交,产物 round-robin)→ 跑完后用修复版重提 day17**(与 18~23 并行,成本低);②随纯 SQL 重写已消失(现文件无 batch_fn/mapInPandas);③④保持 TODO(不影响正确性)。day17 健康度:11:31 已落 samples/link_window(第一阶段完成),curves 窗口链进行中,vcore 持续 ~78 核满载。
