# 9.8 进度日志(本日首次会话自动创建于 10:38)

> 随做随记;会话结束前补全"当前状态与下一步"。记法沿用 9.4progress.md。

## 今日事项

### v2.1 合并式管线收尾(承接 9.7 §16)

1. **流式训练 OOM 三连查(终因找到)**:`CurveShardDataset.__iter__` 切批逻辑错——每进 2048 行只切一个 512 批,**剩余全留在 carry 里滚雪球**(每轮净增 1536),收尾时把 34 万行当一批吐出,注意力矩阵单次要 20GiB。判据:probe 探针显示 batch224 形状 (342054,40) 一锤定音。修复:`while n_carry >= batch_size` 循环切净。修后 stream 模式全链 exit 0。
2. **stream 模式收敛性验证通过**:400k 行/epoch × 2 epoch,val_rec 0.0062 ≈ npz 路径同语料 0.0068,CLS 信息比 ×13.9~14.6,acc 与 npz 同水平——**流式与物化语料训练等价**。前置修的两个小 bug:DataLoader 默认转换会把 numpy 字典 tensor 化(collate_fn=identity 修复);stream_batches 产出顺序 (s,v,l,y,m) 与解包 (s,v,l,m,y) 错位。
3. **用户否决打包 python 环境**:minipy3 无 pandas/numpy(实测 tar 清单,只有 flask/redis 类杂项),mapInPandas 在 yarn 会挂;用户要求"用 pyspark 适配"→ **build_curves_spark.py 重写为纯 Spark SQL**:窗口函数(pre_cum 比例前缀和/样本级聚合)+ collect_list(struct) sort_array + transform()/aggregate() 高阶函数生成 speed/valid 数组,**executor 端零 python**,minipy3 直接可用。yarn 资源较 ingest 调大(executor 8g+overhead 4096,driver 12g——窗口+groupBy 链条 shuffle 更重)。
4. **`scripts/submit_curves_yarn.sh` 落地**(三档 local/dry/yarn,套路同 ingest 版);dry 档验证路由正确。待 SQL 版本地等价验证过 → 首提 day17。
5. 待办:SQL 版 curves 本地跑+逐键等价(vs profiles npz,判据同 9.7 §16:键/标签逐位、speeds 容差 3.8e-6)→ yarn 首跑 → 7 天曲线分片 → stream 正式预训练(入口脚本现成,语料路径换 curves 分片目录)。
6. **纯 SQL 版本地验证完成(61 stage,~40min,3 文件)**:行数 3,653,953 与参照 npz 精确一致;键集合/lengths/valid/n_bins 完全一致;y 在 f32 对齐后逐位一致(npz 存 f32,SQL 版保留 double,直接比有 6e-5 假差异——比较时必须对齐精度,踩坑记档);v/td 容差 1e-4 内全过;speeds 与 pandas 版同一差异模式(26.5% 位置 ≤3.8e-6,T_diff f32 舍入,SQL 版更准)。**判决:v2.1 合并管线(纯 SQL)语义等价,executor 零 python,可上集群**。另:pandas 的 `df.sub` 是减法方法名,取列必须用 `df["sub"]`。
7. **yarn 首跑 day17 已提交**(11:0x):`tl_curves_20260817` = application_1782979140233_46705854,SUBMITTED 排队中;输出 `hdfs://.../curves_spark/day20260817`(curves/ + samples.parquet + link_window.parquet)。跑通后逐天提 18~23。本地验证使命完成,后续大数据全在集群。

## 1. stream 链路审查 + CUDA OOM 根因与修复(晚会话)

**用户新增 v2.1 stream 链路**:`build_curves_spark.py`(一个 Spark 作业:ingest 语义 + mapInPandas 跑 per-sample curve 逻辑,直出 curves/ parquet shards + samples/link_window + edges 元数据)→ `pretrain_stream.py`(IterableDataset 流式消费)→ `train_pretrain.py --config pretrain_stream.yaml`(mode=stream 分支)。smoke3 跨链路验证:curve 数 3,653,953 与 profiles 链完全一致 ✅。鲁班 k8s-job(npz 路径)8 epoch 跑通(app k8s-job-2mholm,sacc_hour=1.0 系单小时语料 artifact)。

**stream 训练 OOM 根因**(smoke2/smoke3 两次确定性复现,数字一致:已分配 33.85GiB + 请求 20.06GiB):
- 关键算术:20.06GiB = 73,344²×4B **精确相等**,73k ≈ val 行数(2% shards)→ eval 快速路径里产生了 [N_val×N_val] fp32 方阵
- 落点 `torch._transformer_encoder_layer_fwd`(nn.TransformerEncoder 的 MHA fused fastpath,仅 eval+no_grad 走);实测该路径单次前向 workspace 比关闭大 8 倍(1.095 vs 0.135GiB)
- 孤立实验三连:train-only 平/val-only 平/短train+val 活 → 需完整 train 阶段在前才触发(具体保持机制未完全定位,但方阵尺度∝val_rows²)
- **不能裸搬 A100-80G**:54GB 峰值本卡放得下,但方阵 ∝ val²;7 天语料 100 万行 val → 1M²×4B=4TB,任何卡都死

**修复**(已落地 train_pretrain.py):①`torch.backends.mha.set_fastpath_enabled(False)`(__main__,附测量注释);②`_metrics` 开头 `torch.cuda.empty_cache()`(训练段碎片保险)。**验证**:原场景(400k 训练行+全量val)从必炸 → 稳过,GPU 峰值 1.080GiB(50× 余量),val_rec 0.0064/cls0 x13.51/7.12min。上集群前提已满足。

**审查遗留 P1(未修,下次)**:①stream 的 train/val 按 shard 切分,但 curves 写出是 round-robin repartition → 同一 sample 的多条 curve 可能跨 shard → **sample 级防泄漏保证被破坏**(建议读侧按 hash(sample_id) 过滤,或写出改按 sample_id 分区);②build_curves_spark `batch_fn` 的 pending 边界分支 `pending.append(chunk)` 未先 clear → 跨 ≥3 个 chunk 的 sample 会丢中间 chunk 行(当前 chunk=10000 行/sample~几十,实际不可达,一行修复);③`workers` 配置读了没用(DataLoader 硬编码 num_workers=0);④build_curves_spark 的 dedup persist(DISK_ONLY)未按 local master 跳过(同 9.7 的配额坑)。

**P1 处置(11:4x,午间会话)**:①已修——`build_curves_spark.py` curves 写出改 `repartition(n, sample_id)`(hash 分片,一样本一分片,shard 级 val 切分恢复样本级防泄漏);**day17 在跑的是修复前版本(11:22 提交,产物 round-robin)→ 跑完后用修复版重提 day17**(与 18~23 并行,成本低);②随纯 SQL 重写已消失(现文件无 batch_fn/mapInPandas);③④保持 TODO(不影响正确性)。day17 健康度:11:31 已落 samples/link_window(第一阶段完成),curves 窗口链进行中,vcore 持续 ~78 核满载。

8. **代码基线已同步 GitHub**(commit `86a08b7` "v2.1: unified Spark pipeline (pure SQL) + streaming pretraining"):51 文件 +4544/−138,main 与 origin/main 一致。覆盖 v2.1 合并式管线/流式预训练/CurveMAE/Level2 全部代码+配置+进度 md;legacy V1 归档(数据留服务器),`legacy/data/` 已入 gitignore。**注:day17 跑的是修复前 round-robin 版(11:22 提交的代码),当前 86a08b7 已含 sample_id 分区修复 → day17 跑完重提时必须用本基线**。

## 附:codex 远程连本 pod 排查结论(非实验,备忘)

想装 OpenAI Codex 到本 pod 远程用,结论:**两道平台硬墙,基本不可行**。
1. **SSH 免密无门**:pod sshd `authenticationmethods keyboard-interactive` + PAM `luban_pam_plugin.so`(Luban LES,双因素:D盾+LES 密码),不放行 publickey;authorized_keys 配了也无效(实测)。平台文档:密码不可改、无公钥入口。别改 sshd/PAM 绕过(安全红线 + pod 重建即回滚)。
2. **网络拦 OpenAI**:pod 内直连 `auth.openai.com`/`api.openai.com`/`chatgpt.com` 全 403(公司白名单);本地代理 127.0.0.1:7897 也出不去 → codex(OpenAI 云账号)在 pod 上无法登录/调模型。对比:Anthropic(Claude Code)在此网络可用。
- 已做:`~/.ssh/authorized_keys` 加过又删掉(恢复原状);codex 重装为可用 `codex-cli 0.153.4`(在 `~/.nvm/.../v24.14.0`,未登录)。npmmirror 直连可达,走 7897 代理更快。
- 若以后真要 pod 上跑 OpenAI 系,前提是搞到一条 pod→OpenAI 的合规代理/内网网关,再 `codex login`。

9. **day17 yarn 首跑完成(12:02)**:`tl_curves_20260817` SUCCEEDED——samples(11:31)/link_window(11:34)/curves 200part·17.6GB(11:40 _SUCCESS)/curves_meta(12:02)。它是 **11:22 修复前 round-robin 版**,按 §6 决议待用 86a08b7(sample_id 分区)重提,暂不用于训练。
10. **新方案讨论(午后)**:用户考虑不走 7 天逐日全量 build+再下采,而是**直接按"每条完整 link 7天内顶 ≤N 条轨迹(整车保留)"下采样 raw → 再 build → CLS 预训练**(N 可扫)。语义确认:raw 里 **link=完整 target_link_id(≈静态 beijing_links)、sample=一条轨迹(一个 traj)穿过一条 link 的一次、sub=仅>200m link 派生的 200m 切分**;per-full-link 顶 N ⇒ 每个 200m sub 曲线数也 ≤N(同轨迹对同一 sub 只产 1 曲线)。已知坑:`--sample-fraction` 是行级会拆散 sample,不能用于 per-link 顶 N;需给 build_curves_spark 加 sample 级 `--max-trajs-per-link`。**18~23 暂不提,等该开关落地后改单次 7 天限量作业(edges 也要在全语料上算一次)**。
11. **7 天数据摸底(为限量决策)**:静态 `beijing_links`(map_version=2026081412)= **2,976,427 有向 link**;长度:59%≤50m、中位 38m、均值 68m、最长 12.7km、>200m 仅 5.8%(17.3w)。官方 per-hour reports 7 天合计:**n_trajs 33,547,341(~3355 万,日均~480 万)/n_samples 11.4 亿(日~1.63 亿)**;本地 1.1h 验证(target_link 37.4w 全部 ⊆ 静态 2.98M,100% 对上)。同事说法对照:总 link~150w ≈ 2.98M/2(按无向/双向),热 link 10-20w 待 7 天逐 link 统计验证。
12. **7 天逐 link 普查作业已提 yarn**(`tl_week_links_7d` = application_1782979140233_46817026,RUNNING):读 168 时 raw→只投影 4 列+seg_mark==1→去重到 pass→per-link n_passes/n_trajs(车次)+ 7 天 distinct links/trajs/passes + per-day。工具 `tools/stats_week_links.py`、提交 `scripts/submit_week_stats_yarn.sh`(纯 SQL,minipy3)。单文件验证:distinct traj 与官方 n_trajs 该时吻合(14,413/文件×15≈21.6w vs 官方 21.9w)。

13. **7 天普查结果出(link_stats/day_counts 已拉回,summary 收尾中)**:7 天被 ≥1 次经过的完整 link=**1,808,031**(占静态 298 万 ~61%);每条 link 7 天被不同 traj 经过:median 13/p90 812/max 10.7 万;车次集中度:前 10 万 link 占 78.5%、前 20 万 90.7% → **同事"热 link 10-20 万"基本成立(全图 298 万,前 10-20 万承载绝大多数车流)**。限量效果(per-link cap 住 distinct traj):C=10 → 保留 1258 万经过(1.3%),C=20 → 2123 万(2.1%)。注:普查 glob 误收 8/24 少量小时(口径 17-23 应略小,量级不变)。
14. **traj_id 语义存疑(待确认)**:单日 distinct traj ~39-40 万,但官方"行程数"每天 ~500 万 → traj_id 疑为**持久车辆/司机 ID** 而非单次行程。影响"一条 link ≤N 条轨迹"按谁数。本地跨天重叠检查被搁置(用户叫停),留待与数据方确认。
15. **改造交接文档已产出**:`target_link/改造方案.md`(给 Codex,自包含):现状/名词+实测数字表/Step1-5 任务(核心=`build_curves_spark.py` 加 `--max-trajs-per-link N`,sample 级 per-target_link、整车保留、确定性伪随机,施加于 seg_mark==1 后 dedup 前;`--inputs` 改逗号多 glob;edges 单语料一次算)/数据怎么读(本地样例路径+raw schema 全列+产出契约+local|dry|yarn 跑法)/已知坑 9 条/Open Questions 6 条(①traj_id 语义 ②N ③是否按小时分层 ④7 天口径 ⑤day17 重跑 ⑥规模预算)/验收清单。**代码尚未改(开关未加)**——等 Codex 按文档实施或用户拍板由本会话做。

16. **普查作业 SUCCEEDED + summary 落盘,traj_id 语义判明**:`tl_week_links_7d` SUCCEEDED,summary:distinct link 1,808,031 / **distinct traj_id 753,395(7天~75.3万)**/passes 11.41亿。单日 distinct traj ~31-40万但 7 天并集仅 75 万 ⇒ **traj_id=持久车辆/司机 ID,非行程**(官方 3355 万车次 ≈ 75.3万车×45 车次/车/周)。`改造方案.md` 已同步:§2 加总体 distinct + traj_id 判明;§7-① 更新为"默认按 sample(经过次数)限量 (b),按车 (a) 可选+连带语义说明"。留给用户拍板的只剩:cap 粒度(a/b/c)、N、是否按小时分层、7 天口径排除 8/24。

17. **N 限量开关已实现(代码层,未提交)**:按 改造方案.md Step1-2 在 `tools/build_curves_spark.py` 落地——新增 `--max-trajs-per-link N`/`--cap-seed` CLI 参数 + `cap_samples_per_link(df,n,seed)` 函数(per-full-link cap ≤N 个 distinct sample_id,整车保留:按 (link,sample) 复合键过滤行,同一 sample 的 bin/sub 全保留;单位=一次经过;选择确定性:hash(f"{seed}:{sample_id}")+sample_id 排序取前 N);`--inputs` 改为逗号分隔多 glob(7 天可合并单作业,edges 全语料一次算);`scripts/submit_curves_yarn.sh` 加 `MAX_TRAJS_PER_LINK`/`CAP_SEED` 环境变量透传(local/yarn 两档)+ 头部 week_cap 用法注释。**HDFS `beijing_week_biz/samples/` 目录检查确认:恰 168 个小时分区 = 20260817-23×24h,无 8/24 残留**(当前单个 day-glob 覆盖 7 天干净;仍实现了逗号多 glob 兜底)。**注:doc §2 限量效果数字(C=10→1258 万)当年按 distinct traj(车)算,与 Step1 规格"按 sample(经过次数)"粒度不一致,已按规格 (b) 实现,数字仅量级参考。**
18. **N 限量本地自检通过(真实数据)**:切片 = hour2026082007 两最小 part(~147MB),`--max-trajs-per-link 3` 走真实生产函数校验四项:①N=0 恒等(2,628,664 行 == 输入);②限量生效且不超限(每 link distinct sample 峰值 129→3,219,041 经过→留 168,626);③整车不拆(保留 168,626 sample 逐行数 0 处不一致);④确定性(两次独立 cap 集合对称差 0)。判断:`cap_samples_per_link` 逻辑 + 逗号多 glob + 脚本透传就绪,可上集群 7 天单作业 MAX_TRAJS_PER_LINK=10(OUT_DIR 建议 `.../curves_spark/week_cap10`)。代码未 commit/推送,集群作业未提(等用户拍板)。
