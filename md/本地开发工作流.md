# 本地开发工作流

写给"在本地笔记本改码、在服务器跑"的场景。本地看不到数据集、HDFS、集群队列、
k8s 平台，所以本文把**只能从服务器侧看到的事实**写成硬约束，再给往返流程。

一句话流程：**本地改 → `pytest tests/test_cell_mae.py`（不需要数据）→ push
→ 服务器 `git pull` → 提交对应 sh → 看 NFS 上的 metrics.jsonl。**

---

## 一、数据契约（本地绝对看不到的部分）

### 1.1 语料在哪

| 东西 | 位置 |
|---|---|
| 全量语料（HDFS） | `hdfs://DClusterNmg3/user/bigdata-dp/user/junao/target_link/corpus_v1` |
| 训练侧小样本（NFS，已落地） | `$REPO/runtime/cell_smoke_20260821_64_67/{train,val}/{observations_v2,training_groups}` |
| 上游 raw samples | `hdfs://DClusterNmg3/user/bigdata-dp/user/liruifeng/traffic_traj_encoder/beijing_week_biz/samples` |

`corpus_v1/` 下三个目录，**互相独立、可单独重建**：

- `observations/` — 旧版，**没有 `bin_pos` 列**，训练读不了。
- `observations_v2/` — 现役，训练只读这个。
- `cells/` — 验收基线（K 直方图），训练不读。
- `training_groups/` — 训练单元。

> **坑**：`--obs-dir` 默认值是 `observations_v2`，但那个目录**曾经**叫
> `observations`（且旧的那份没有 `bin_pos`）。改名是**故意没做完**的：
> 现役 `observations/` 还在服务训练，不要单方面 `mv` 覆盖它。重建新 obs 时
> 用 `OBS_DIR=<侧目录>` 写到旁边，验完再换。

### 1.2 一行 = 一个 (cell, 轨迹) 观测，ragged

`observations_v2` 的列（`tools/build_corpus.py` 的 docstring 是权威版本）：

| 列 | 类型 | 含义 / 陷阱 |
|---|---|---|
| `cell_id` | int64 | `xxhash64("map_version\|target_link_id\|seg_idx\|window")` |
| `sample_id` | string | 上游 `traj_id#link#pass#event_hour` |
| `dt` | float | `t_seg_enter - window_start`，**在 [0,600) 内**，600.0 会被 reader 夹到 `nextafter(600,0)` |
| `n_pieces` | int16 | ragged 长度，≤50 bins，实测最大 51（一个 bin 被链路边界切开会多一片） |
| `T_diff[]` | float32 | 逐片通行时间，**NaN 原样保留** |
| `ratio_pct[]` | uint8 | 逐片占其 10m bin 的比例 ×10，∈ 1..10 |
| `observed[]` | bool | 该片有 GPS 定位 |
| `valid[]` | bool | `T_diff` 不是 NaN |
| `bin_pos[]` | uint8 | **空间 token 位置，0..49**（见下） |
| `map_version`/`target_link_id`/`seg_idx`/`window` | | 冗余存下来，后面几级不再读 raw |

**`bin_pos` 是绝对栅格，禁止按轨迹重归一化。** 一条轨迹穿过 500m 段 = 50×10m bin；
raw `bin_idx` 是全走廊编号，`seg_idx` 是 500m 段号，实测恒等式
`(bin_idx - 10) // 50 == seg_idx`（100%，4 天 57M 行），于是
`bin_pos = bin_idx - 50*seg_idx - 10` 恰好落在 [0,49]。
栅格为同一 cell 的所有轨迹共享：seg_idx=3 时 1959/1960 条样本从 `bin_idx=160`
起步，有 1 条从 163 中途汇入 —— 一旦按轨迹重归一化，这一条会整体错 3 个 bin。
`collate_cells` 会 assert 0..49。

### 1.3 valid / observed / present 是三件事（最容易改错的地方）

- `observed` = **GPS 有定位**。
- `valid` = **T_diff 已知**。corpus 从不插补。
- 折叠到 bin 级后：`bin_valid = present & (该 bin 的所有片都 valid)` ——
  必须**全部片**都 valid，因为 `T_diff` 是**求和**，丢掉一个 NaN 片会静默低估。
- `present` = 该 bin 至少有一片。reader 里 `present = ratio > 0`（每片 `ratio_pct ≥ 1`，
  缺席的 bin 被写成全零）。

**piece → bin 的折叠规则**（`cell_corpus._scatter`，改 reader 时不许动）：

| 量 | 规则 |
|---|---|
| `T_diff` | 各片**求和**（NaN 片由 `where(v, t, 0)` 挡在外面） |
| `ratio` | 各片**求和** |
| `observed` | 各片**取或** |
| `bin_valid` | **全片 valid** 才 valid |
| 无片的 bin | 全零 + invalid |

折叠后 `x` 的第 0 列对 invalid bin 是 0，而 `ratio`/`observed` **保留**——
所以 `bin_valid` 必须作为**第 4 个输入通道**喂进模型，否则"T_diff 未知"和
"T_diff 真的是 0 秒"无法区分（§10 的修复，回归测试见
`test_bin_valid_is_a_real_input_channel`）。

**注意力 mask 用 presence，不用 validity**：invalid bin 只是时间未知，
geometry（ratio/observed）还在，必须继续参与注意力。把两个 mask 合成一个，
会静默删掉 reader 的"invalid bin 保留 ratio/observed"契约
（回归测试 `test_invalid_bin_still_attends`）。

### 1.4 NaN 不是 null（踩过一次）

**缺失值是存在非 null 槽位里的 IEEE NaN，不是 Arrow null。**

```python
pc.is_null(t)   # → 0 行  ← 骗人的
np.isnan(t)     # 或 pc.is_nan(t) → 真实数量
```

实测 63,457 个 IEEE NaN 片 == 全部 17,587,806 片中 `valid=0` 的片数，逐位相等。
任何 QC/统计脚本判缺失**必须用 `is_nan`**。用 `is_null` 会得出"没有缺失值"的错误结论
并紧接着在 `bad[0]` 上 IndexError。

本地分区实测：`valid=0` 占 0.36%/0.35%/0.36%/0.35%/0.34%，`observed=1` 占 38.3%/38.3%/38.2%/38.2%/37.7%。

### 1.5 group 策略（策略在 reader，不在语料）

- 一个 **cell** = `(map_version, target_link_id, seg_idx, 10min window)`。
- 一个 **group** = 一个 cell 的最多 `m_max=16` 条轨迹；`K_min=4`。
- K>16 时按 `hash("cell|sample_id")` 确定性乱序后**均分**成 `ceil(K/16)` 组
  （实测 K=28 → 2 组），每条轨迹恰好用一次。
- 50% **整条轨迹**掩码，由 `(epoch, group_id)` 决定 —— 同一 epoch 重跑完全一致。
  换 epoch 要调 `set_epoch`。
- padding **只在 collate 发生**，语料里不物化 16 槽。

### 1.6 张量契约

`collate_cells` 返回 `[B, 16, 50, F]`，`F = (T_diff, ratio, observed) = 3`：

| key | 形状 | 说明 |
|---|---|---|
| `x` | `[B,M,50,3]` float32 | 原始特征 |
| `bin_valid` | `[B,M,50]` bool | 该 bin 的 T_diff 可信 |
| `traj_valid` | `[B,M]` bool | 槽位是真实轨迹（padding 为 False） |
| `delta_t` | `[B,M]` float32 | 秒 |
| `mae_mask` | `[B,M]` bool | 整条轨迹被掩码 |
| `m_max` / `n_bins` | int | 16 / 50 |

**padding 槽位约定**：全零 + `traj_valid=0`，且 `mae_mask` **永不**覆盖 padding。

### 1.7 冻结结构

`md/9_4.md:44-53` 钉死的一级结构：
`Input MLP → Position Embedding → Transformer Encoder → valid-bin Mean Pooling → r_k ∈ R^128`。
`Codex` 复核时指出过两个偏离点（时间嵌入须是 `delta_t/600 → 小 MLP`；轨迹编码器
须有 bin 级 Transformer），现已在 `cell_mae.py` 收敛。

---

## 二、需要改动的地方（截至 2026-09-10，未完成项）

| # | 位置 | 要改什么 | 为什么 |
|---|---|---|---|
| 1 | `md/最新讨论想法.md:127` vs `cell_corpus.py` | 冻结文本写的是 `[T_diff, ratio, valid]`，实现是 `[T_diff, ratio, observed]` | 必须二选一，否则"冻结结构"名不副实 |
| 2 | `cell_corpus.py` `_scatter` | `present` 现在算出来又被丢掉，注意力 mask 依赖 `ratio > 0` | 显式穿一个 `bin_present` 字段出来，别靠不变量 |
| 3 | `tools/train_cells.py` 日志 | `supervised_groups` 统计的其实是 **batch 数** | 名字骗人，要么改标签要么改统计 |
| 4 | `cell_mae.py` docstring | 已重写（§10） | 完成 |
| 5 | `tools/check_cell_batch.py` | 真实语料单批次验收还没跑 | 本地跑不了（要 HDFS） |
| 6 | 全语料 `min(bin_pos) >= 0` 扫描 | 未跑 | 同上 |

---

## 三、本地能做什么、不能做什么

**本地能做（不需要数据，秒级）：**

- 改 `target_link_v1/models/` 全部模型代码。
- 改 `target_link_v1/data/cell_corpus.py` 里的**纯函数部分**：`_scatter` 的折叠数学、
  `collate_cells` 的 padding/mask 逻辑 —— 只要构造 `np.ndarray` 就能测。
- 改 `tools/` 下的训练循环（`train_cells.py`），但只能靠读代码，跑不了。
- 跑 `pytest -q tests/test_cell_mae.py`（12 个用例，合成张量，CPU，约 1 分钟）。

**本地不能做：**

- 读 HDFS / 看真实 parquet（**且本 pod 的 pyarrow-over-HDFS 已坏**）。
- 提交任何训练（规矩：**不在本机跑训练**）。唯一例外是纯 CPU 的形状/单元检查。
- 判断数据结构对不对 —— 那必须由服务器侧的验收脚本回答。

**要读 HDFS parquet 时的唯一正确姿势**：先落地再读。

```bash
hdfs dfs -get /path/to/x.parquet /tmp/x.parquet
python your_script.py --input /tmp/x.parquet
```

pyarrow 直连 HDFS 需要 JVM classpath，实测不可用：

```bash
export CLASSPATH=$(hadoop classpath --glob)
export ARROW_LIBHDFS_DIR=/usr/local/hadoop-current/lib/native
```

---

## 四、往返流程

### 4.1 本地（改 + 自测）

```bash
cd <本地仓库>
git pull                       # 先拉，避免分叉
# ... 改码 ...
PYTHONPATH=$PWD python -m pytest -q tests/test_cell_mae.py
git add -A && git commit -m "..." && git push
```

> 本地跑 pytest 需要装 `torch`+`pytest`。测试里 `out["prediction"]` 之类全是合成张量，
> 不碰数据集，所以本地结果和服务器一致。

### 4.2 服务器（拉 + 跑）

```bash
ssh <服务器>                    # GitHub 走 ssh.github.com:443，见 ~/.ssh/config
cd /nfs/dataset-ofs-494-1/project/user/junao/target_link
git pull
```

**按改动类型选入口：**

| 改了什么 | 跑什么 |
|---|---|
| 模型 / 训练循环 / reader | k8s 平台：启动命令填 `bash /nfs/.../target_link/scripts/submit_cell_smoke_job.sh`，资源 1×GPU 单机(A100) |
| 语料构建（Spark） | `MODE=yarn bash scripts/submit_build_corpus_yarn.sh` |
| 只想验 reader 字段 | `python tools/check_cell_batch.py --root hdfs://.../corpus_v1 --obs-dir observations_v2 --batch 4` |

**k8s 平台规则（硬约束）：**

- 启动命令**只能是一个 sh 脚本路径**；所有参数必须写死在 sh 内部、有默认值、
  不依赖任何预先 export 的环境变量。
- 想要新参数组合，**改 sh 脚本里的 `export`**，不要在平台上拼命令行。
- 日志直接落 stdout，产物落 NFS `runtime/`（已被 gitignore）。

### 4.3 看结果

```bash
ls -t runtime/cell_train_smoke_a100_*/          # 最新一次
cat runtime/cell_train_smoke_a100_*/metrics.jsonl
```

`metrics.jsonl` 每 epoch 一行；关注 `loss` / `aggregate_gap`。

**`aggregate_gap` 的正确读法**：它是"消融 level-2 状态后的 loss"减"真实 loss"。
gap 变小**不等于**聚合被削弱 —— 也可能是被消融的一侧自己在变好。要看两侧各自的绝对值。

**`supervised_groups` 标签目前骗人**（见 §二.3），它数的是 batch。

---

## 五、提交纪律

**绝对不能进 git**（`.gitignore` 已覆盖，但手滑 `git add -f` 会破功）：

`runtime/`、`data/`、`legacy/data/`、`*.parquet`、`*.npz`、`*.snappy`、
`runs/`、`checkpoints/`、`*.ckpt`、`*.pt`、`*.log`、`__pycache__/`

**归档原则：只移不死。** 旧代码移进 `legacy/`，不删。详见 `legacy/README.md`。
