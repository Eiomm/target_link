# 目标组件与 ratio：两文件全量审计

> 来源核对更新：随后直接列出 HDFS `beijing_week_biz/samples/event_hour=2026082007`，当前文件 UUID 为 `03e36ee8-817b-4cf8-99fa-ad0a18eaa7aa`，与本次本地审计文件 UUID 不同。以下数字仅适用于本地两文件；尚未确认它们与当前 biz 表内容一致，不能据此修改 biz 生产适配规则。

2026-09-09。只读审计，未修改适配器，未重建 v4 数据。

输入为 `data/raw_hdfs/event_hour=2026082007/` 中 v4 metadata 指定的 part-00000 和 part-00001（文件 UUID `4654cc31-8ebf-4f52-849e-9e2748fc665d`）。

候选规则：`seg_mark=1 AND link_id=target_link_id`；距离使用原始 `bin_size_m*ratio`。按 `(map_version,target_link_id,sample_id)` 聚合；闭合在任何时间质量或尾段过滤之前计算。

| 指标 | 数值 |
|---|---:|
| 过滤后经过数 | 1,947,941 |
| 过滤后组件行数 | 24,217,585 |
| 重复 bin KEY 数 | 0 |
| 非法 ratio 行（null/NaN/≤0/>1.000001） | 0 |
| bin 不连续经过数 | 0 |
| 同次经过 L_link_m 不一致数 | 0 |
| 距离和与 L_link_m 差超过 1cm 的经过数 | 1,087,408 |
| 距离差 p0.1/p1/p50/p99/p99.9，单位米 | -5 / -5 / -1 / 4 / 4 |

## 真实边界反例

- `8015187109577216227#5508990#000`：L=106m，seg1 目标行 11 个完整 10m bin，所有 ratio=1，合计110m。相邻 seg0 bin9、seg2 bin21 也标相同 link_id 且 ratio=1。因此不能直接改成对全走廊按 link_id 取行，也不能凭 ratio 重建106m精确边界。
- `12609403130832652590#298801410#000`：L=191m，seg1 目标行合计190m；剩余1m在 seg2/bin29，link_id仍是目标路，ratio=0.1，T_diff=0.092s。同 bin 另一 link 占0.9，T_diff=0.832s。
- `5830315425077720592#403855250#000`：L=233m，seg1 目标行合计230m；剩余3m在 seg2/bin33，link_id仍是目标路，ratio=0.3，T_diff=0.285s。同 bin 另一 link 占0.7，T_diff=0.665s。

## 结论与约束

120条有异link拆分的定向抽样全部闭合，不能推广到所有经过。全量审计揭示 seg_mark 与组件几何边界不完全一致。ratio 是组件占格比例；归属还依赖 link_id，精确目标段边界还需要独立定位。不得直接合并所有seg1同bin组件，也不得盲目收取所有同link_id的走廊行。不得修改ratio或按比例缩放T_diff来强制闭合。

候选过滤规则解决了这两个文件内的重复KEY，但没有通过精确几何验收，因此本轮不把它直接上线到适配器。下一步需要确定目标路起止位置的上游生成规则，特别是整格取整/延伸与 seg_mark 的赋值方式；也可另行定义明确的近似网格契约，但不能沿用“精确静态位置已确认”的声明。
