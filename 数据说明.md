# 数据说明（自包含，给读不到集群数据的 Codex / 新会话）

> 生成 2026-09-09。本文档描述 target_link 项目的**全部数据资产**：原始表、静态表、本地样例、各阶段派生契约。
> 所有数字均为服务器实测（标注了来源），不是估计。你（Codex）在本地读不到 HDFS / 真实数据——
> 这份文档 + 仓库内的本地样例就是你能看到的全部，写代码前先读完 §5"语义真相"和 §11"坑清单"。
> 仓库根：`target_link/`；GitHub `Eiomm/target_link`；当前主线分支 `feat/window-partial-trajectories`。

---

## 0. 你的能力边界

- **你能看**：①本文档；②本地样例 `data/raw_hdfs/`（6 个小时的原始表副本 + 散件，见 §8）；
  `data/raw_hdfs/data.txt`（一条 producer 原始消息的完整解码，§3 全文解剖）；③仓库内所有管线代码与测试。
- **你看不到**：HDFS 上任何东西（原始 7 天全量、静态表、yarn 产物）。
- **分工**：你写代码/脚本 → 用户同步到服务器跑 → 验收数字回填。文档里 §12 的硬数字就是验收锚点。
- **集群红线**：executor 端零 python（minipy3 无 pandas/numpy），只能纯 Spark SQL / `pyspark.sql.functions`。

---

## 1. 数据全景（一条链）

```
HDFS: beijing_week_biz/samples/event_hour=YYYYMMDDHH/part-*.snappy.parquet   ← 原始表(7天×24时,841G)
        │  40 列 bin 级走廊观测(§4)；每行 = 某车一次经过的走廊中一个 10m bin
        ├─ tools/ingest_spark.py ──→ processed 三表(samples/bins/link_window)      [V1,已等价验证]
        ├─ tools/build_curves_spark.py ──→ curves 语料(+samples/link_window/meta)  [曲线预训练线,9.8 起降级]
        └─ tools/adapt_samples_windows.py ──→ events 表(15 列,§9.3)                [windows 主线,9.9 落地]
                  │
                  └─ tools/build_windows_spark.py ──→ windows corpus(§9.4) ──→ train_windows.py
HDFS: beijing_links/map_version=2026081412/*.parquet                          ← 静态 link 表(297.6万,§7)
```

业务目标一句话：从"车穿过 link 的 bin 级走行观测"学**路段×时刻的路况表征**（在线因果快照，windows 主线），
下游接 ETA。数据是滴滴北京轨迹的走廊切分产物（producer 更上游：轨迹 → 地图匹配 → 按候选 target link 切走廊 → 10m bin 化）。

---

## 2. 原始表 samples：物理组织（hdfs dfs -ls 实测 2026-09-09）

```
hdfs://DClusterNmg3/user/bigdata-dp/user/liruifeng/traffic_traj_encoder/beijing_week_biz/samples/
├── _SUCCESS                                   (2026-09-05 19:40)
└── event_hour=2026081700 ... event_hour=2026082323     ← 168 个小时分区,整 7 天(8/17~8/23)
    ├── _SUCCESS
    └── part-00000-<uuid>-c000.snappy.parquet ... part-00014   ← 15 个 part/时
```

| 项 | 实测值 |
|---|---|
| 分区数 | 168（2026081700–2026082323，无缺无多；早前担心的 8/24 残留**已不在**） |
| 单时体积（2026082007） | 6.4 G 压缩 / 19.1 G 未压缩 |
| **总体积** | **841.1 G 压缩 / 2.5 T 未压缩** |
| 单 part 行数（本地副本实测） | part-00000 = 34,827,588 行 → 单时约 5 亿行 |
| 存储格式 | snappy parquet，分区列 `event_hour int32`（仅 HDFS 有，本地副本无，见坑#3） |

**官方逐时口径锚**（`.../beijing_week_biz/reports/report_YYYYMMDDHH.json`，字段 `n_trajs`=车次、`n_samples`=经过数）：
7 天合计 **3,354.7 万车次 / 11.42 亿次经过**（逐日：8/17 490.6万/1.647亿 … 8/23 420.9万/1.313亿，峰值 8/21）。

---

## 3. 一条样本的完整解剖（producer 消息 → bin 行）

`data/raw_hdfs/data.txt` 是 producer 端**一条原始消息**的解码（数组格式，尚未展平成表）。
以它为标本，全部数字闭环验证过——**这就是原始表每列语义的权威来源**。

消息头部（sample 级）：

```
traj_id       10007212890835753880     # 车(见 §5-①)
pass_idx      0                        # 该车当天第几次经过(去重序号)
target_link_id 125121851               # 本条样本的 target link
map_version   2026081412
bin_size_m    10.0                     # bin 空间粒度,全表恒 10
L_link_m      1455.0                   # target link 静态长度
n_bins        166                      # 走廊总 bin 数 = ceil((1455+100+100)/10)
n_segs        3                        # target link 由 3 个物理 link 组件组成(§5-④)
t_enter       1786848558.04            # 进入 target 本体时刻(epoch 秒)
t_ref         1786848549.71            # 走廊起点时刻,T_cum 的零点(比 t_enter 早 8.32s=上游缓冲耗时)
y_travel_s    119.73                   # ★标签:穿过 target 本体的耗时
corridor_time_s 133.61                 # 全走廊耗时 = 末行 T_cum(133.613,精确吻合)
X_in_m/X_out_m 100.0/100.0             # 上/下游缓冲长度(上游恒 100m,§6)
truncated_in/out False                  # 进/出走廊时轨迹是否被截断
n_pts_corr    44                       # 走廊内 GPS fix 数 = bin_pt_ts 非 '-1' 的行数(精确吻合)
n_pts_raw/kept 3504/2463               # 整条轨迹原始/地图匹配保留点数
dir_p10/p90   250.25/250.66            # 航向角十分位(度)
```

消息体（bin 级平行数组，`link_seq=['125121861','125121851','7524951','300163181','300163171']`）：

```
seg_mark[i]  ∈ {0,1,2}   0=上游缓冲(10 bin) 1=target 本体(141.5 bin) 2=下游缓冲
seg_idx[i]   -1=缓冲区; 0/1/2 = target 本体内第几个物理组件(n_segs=3)
bin[i] 展开: links=[...], codes=[...], ratio=[...], T_cum=[...], T_diff=[...], pt_ts
```

**关键行示例（直接摘自 data.txt）**：

```
bin[ 12] links=['125121851'] ratio=[1.0] T_cum=[10.642] T_diff=[0.683] pt_ts=1786848560   # 普通 target bin
bin[155] links=['125121851','7524951'] ratio=[0.5,0.5] T_cum=[128.041,128.359]
         T_diff=[0.318,0.318] pt_ts=1786848678    # ★一个 bin 被两个物理 link 分摊!
bin[157] links=['7524951','300163181'] ratio=[0.4,0.6] ...   # 下游缓冲里也有分摊
```

**展开成表**：每行 = (sample_id, bin_idx, 该 bin 内每个物理 link 组件一行)，
`sample_id = f"{traj_id}#{target_link_id}#{pass_idx:03d}"`（如 `7210010177485051719#2658440#000`）。

**闭环验证（你写解析代码后应能复现）**：
1. `Σ(bin_size_m × ratio)` over seg_mark==1 行 = `L_link_m` = 1455.0（10..154 全 1.0 + bin155 的 0.5 → 145×10+5）✓
2. `t_ref + T_cum[i]` ≈ 该 bin 下游 fix 时刻 `bin_pt_ts[i]`（偏差 ~0.1s）✓
3. `T_diff[i] = T_cum[i] − T_cum[i−1]` ✓；`corridor_time_s = T_cum[末]` ✓
4. `n_bins = ceil((L_link_m + X_in_m + X_out_m)/10)` ✓；非 '-1' 的 `bin_pt_ts` 计数 = `n_pts_corr` ✓

---

## 4. 全列字典（40 列，本地 parquet footer 实测；HDFS 另有分区列 `event_hour int32`）

| 列 | 类型 | 语义 |
|---|---|---|
| `sample_id` | string | `traj_id#target_link_id#NNN`，一次经过的唯一键，**训练/限量/防泄漏的原子单位** |
| `traj_id` | string | **持久车辆/司机 ID，不是单次行程**（§5-①） |
| `pass_idx` | int32 | 同车同 link 重复经过的序号（拼进 sample_id 保证唯一） |
| `target_link_id` | string | target link（逻辑路段）ID；100% ⊆ 静态表 link_id（§7） |
| `map_version` | string | 地图版本，7 天全量恒 `2026081412` |
| `bin_size_m` | double | bin 空间长度，全表恒 10.0 |
| `L_link_m` | double | target link 静态长度（米） |
| `n_bins` | int32 | 走廊总 bin 数 = ceil((L+X_in+X_out)/bin_size) |
| `n_segs` | int32 | target link 的物理组件数（≥1；多组件见 §5-④） |
| `t_enter` | double | 进入 target 本体的 epoch 秒 |
| `t_ref` | double | 走廊起点 epoch 秒 = T_cum 零点 |
| `y_travel_s` | double | ★标签：穿 target 本体耗时（秒） |
| `corridor_time_s` | double | 全走廊（含缓冲）耗时 = 末行 T_cum |
| `X_in_m` / `X_out_m` | double | 上/下游缓冲长度；**上游实测恒 100m**（§6） |
| `truncated_in` / `truncated_out` | bool | 进/出走廊时轨迹是否被截断 |
| `n_pts_corr` | int32 | 走廊内 GPS fix 数（= bin_pt_ts 非 '-1' 计数） |
| `n_pts_raw` / `n_pts_kept` | int32 | 整条轨迹的原始 / 地图匹配保留点数 |
| `n_gap_bins` | int32 | 走廊内定位空洞 bin 数 |
| `n_zero_ratio` | int32 | ratio=0 的 bin 数（占位格） |
| `dir_p10` / `dir_p90` | double | 走廊内航向角 p10/p90（度） |
| `bin_idx` | int32 | 走廊内 bin 序号，0 起，**样本内严格连续无 gap**（§6 实测） |
| `sub_idx` | int32 | **物理组件序号（=消息 seg_idx），不是 200m 子段索引！**（§5-③） |
| `link_id` | string | 该行所属物理 link（多组件 target 的组件 link；缓冲区=邻居 link） |
| `link_code` | int32 | 该行物理 link 在 link_seq 中的序号 |
| `ratio` | float | **空间占比** (0,1]：该行物理 link 占此 bin 的长度/bin_size_m。f32！精度坑#6 |
| `T_cum` | double | 走廊起点到该 bin **下游端**的累计耗时（t_ref 相对秒） |
| `T_diff` | double | 该行组件在此 bin 内的走行耗时 = 组件内 T_cum 差分（因果语义 §5-②） |
| `seg_mark` | int8 | 0=上游缓冲 1=**target 本体（管线只留它）** 2=下游缓冲 |
| `seg_idx` | int32 | -1=缓冲；≥0=target 本体内物理组件序号 |
| `observed` | int8 | 1=该 bin 由真实 GPS fix 封口；0=两 fix 之间插值均摊（§5-②） |
| `bin_pt_ts` | string | 封口 fix 的 epoch 字符串，无 fix 为 **`'-1'`（字符串）**，判空别用 isNull |
| `link_length` | int64 | 该行物理 link 的静态长度（米） |
| `link_direction` | int8 | 物理link方向 |
| `link_fc` / `link_speed_class` / `link_lane` | int8 | 道路功能等级 / 限速等级 / 车道数 |
| `link_kind` | string | 路段类型码 |
| `link_urban` / `link_county` | int8 / int32 | 城区标记 / 行政区码（如 110105=朝阳） |
| `link_in_links` / `link_out_links` | string | 该物理 link 的上/下游 link 集合 |

---

## 5. 语义真相（考古结论，全部实证过；违反即 bug）

① **`traj_id` = 持久车辆/司机 ID，不是单次行程。**
7 天 distinct traj_id 仅 **753,395**，而官方车次 3,354.7 万（≈75.3 万车 × 45 次/车/周）。
单日 distinct ~31–40 万、7 天并集仅 75 万 ⇒ 同车跨小时/跨天复用。
**推论**：train/val 按 traj_id 切才能车辆级防泄漏；"per-link ≤N 条轨迹"按 sample_id（经过次数）数与按 traj_id（车）数是两个口径，别混。

② **`T_diff` 的因果性：bin 耗时由下游 fix 封口，均摊插值。**
GPS fix ~3s 一个（≈8 bin/个）；两 fix 之间所有 bin 的 T_diff 是**等分摊**的，
即中间 bin 的耗时要等**空间下游那个 fix 到达才定死**。`observed=1` 标记封口 fix 所在 bin。
**推论**：做在线/因果特征（windows 主线）时，末个 fix 之后未封口的 bin **不可用**（实测 6.9% 行，丢弃是因果正确）；
离线全量消费（curves 线）不受影响。封口滞后 avail−bin_end：p50 0.83s / p90 2.3s / p99 5.1s。

③ **`sub_idx` = 物理组件序号，不是 200m 子段索引。**
与 floor(pos/200) 仅 73% 一致（n_segs=1 时碰巧相等，多组件时错位）。
**200m 子段索引是下游 `build_profiles`/`build_curves` 派生的 `sub` 列**（`floor(cumsum(ratio)×10/200)`），原始表没有。

④ **一个 target_link_id 可由多个物理 link_id 组件组成，且两组件可分摊同一个 bin**（data.txt bin[155] 实证：6m+4m 拼 10m bin）。
直接按 (sample_id, bin_idx) 展开会出现**同 KEY 多行**（实测 2026082007 两 part 冲突 664,761 行）。
下游若要求 bin 键唯一，必须先**按 bin_idx 合并组件行**（dist 求和 / T_cum 取末组件 / observed·avail 取 max），
这正是 `tools/adapt_samples_windows.py` 做的事（§9.3）。

⑤ **`seg_mark==1` 是一切处理的第一道闸**：只留 target 本体（0/2 是上下游缓冲，缓冲内 bin_idx 与本体共用同一序号轴）。
⑥ **重复行**：同 (sample_id, bin_idx, link_id) 可能因 segment 重叠产生完全重复行（V1 ingest 实测影响 34% 样本），去重保留首次。
⑦ **`bin_pt_ts` 的 `'-1'`** 是字符串哨兵，不是 null；`isNotNull` 拦不住 NaN/inf（算术溢出产 NaN，坑#5）。

---

## 6. 数据质量实测（hour2026082007 part0，seg_mark==1 后 1,260 万行）

| 指标 | 实测 | 含义 |
|---|---|---|
| 空间闭合 `Σ(bin_size×ratio)−L_link_m` | 中位 **−1mm**，p1~p99 ±5mm，min −5.9m | 前缀和几何成立；尾部留给 build 校验拒 |
| 上游缓冲 `min_bin`（首个 seg1 的 bin_idx） | =10 占 980,946/980,980 | X_in 恒 100m |
| bin_idx 连续性 | 严格连续，0 gap | — |
| `T_diff` NaN | 0.4% | 丢弃（质量过滤 0.41%） |
| 末 fix 后未封口行 | 6.9% → avail=null | 在线不可用，丢弃（5.8%） |
| 7 天 link 覆盖 | 1,808,031 / 静态 2,976,427（61%） | 39% 的 link 7 天内无一次经过 |
| 每 link 7 天不同车数 | 中位 13 / p90 812 / max 106,809 | 车流极度集中：前 10 万 link 占 78.5%，前 20 万占 90.7% |

---

## 7. 静态表 beijing_links（HDFS）

```
hdfs://DClusterNmg3/user/bigdata-dp/user/liruifeng/traffic_traj_encoder/beijing_links/map_version=2026081412/*.parquet
schema: link_id int64, length int64, direction int8, in_links/out_links list<int64>,
        fc, speed_class, lane int8, kind list<string>, urban int8, county int32, bounds struct<...>
```
共 **2,976,427** 条有向 link。长度：中位 38m / 均值 68m / 最长 12.7km；**>200m 仅 5.8%**（17.3 万，会被切成 200m 子段）。
`target_link_id`(string) 转 int64 后 100% 是其子集。

---

## 8. 本地可见样例（你唯一直接读得到的数据）

`target_link/data/raw_hdfs/`：

| 内容 | 说明 |
|---|---|
| `event_hour=2026081708 / 2026082007 / 2026082008 / 2026082017 / 2026082107 / 2026082117` | 6 个小时的完整副本（各 15 个 part） |
| `part-00002 / 00004 / 00011 / 00014.parquet` | 最早的散件（V1 时代单文件，无目录） |
| `data.txt` | producer 原始消息解码样例（§3 的解剖标本） |

**坑**：本地副本**没有 `event_hour` 列**（那列只存在于 HDFS 分区目录路径上）。要日期从 `t_enter` 推：
`F.date_format(F.from_unixtime(F.col('t_enter') + 8*3600), 'yyyyMMdd')`（+8h 转北京时间）。

本地直读（不用 Spark）：
```python
import pyarrow.parquet as pq
f = pq.ParquetFile('data/raw_hdfs/event_hour=2026082007/part-00000-*.parquet')
f.schema_arrow        # 40 列
f.read_row_group(0).slice(0, 100).to_pandas()   # 前 100 行
```

---

## 9. 派生数据契约（管线各阶段输出）

### 9.1 ingest 三表（`tools/ingest_spark.py`，V1，已与 pandas 版逐位等价验证）
`<out>/samples.parquet`（样本级：sample_id/target_link_id/v_sample=L/ΣT_diff/window_id/y_travel_s…）、
`<out>/bins.parquet`（seg1 去重后的 bin 行）、`<out>/link_window.parquet`（(link,1h窗) 均速/n_trajs）。
历史参照：`data/processed_smoke_spark3/`（单时 293 万样本）。

### 9.2 curves 语料（`tools/build_curves_spark.py`，9.8 起降级备用）
`<out>/{samples.parquet, link_window.parquet, curves/(N 分片), curves_meta.json.d}`。
curves 列：`sample_id,sub,link_id,y,td_target,n_bins,v_sample,window_id,speeds,valid,observed,length,eff_len_ratio`；
**分片必须 `repartition(n, sample_id)`**（防 train/val 样本级泄漏）；meta 的分位 edges 必须单语料一次算。
支持 `--max-trajs-per-link N`（sample 级 per-target_link 确定性限量，hash 排序取前 N，整车保留）。

### 9.3 events 表（`tools/adapt_samples_windows.py`，**windows 主线的输入**，2026-09-09 落地）
把原始表适配成 build_windows 的 15 列契约（4 列缺口的推导全在这）：

```
map_version, target_link_id, sample_id, bin_idx(int64), ratio(double), bin_size_m,
T_diff, L_link_m, spatial_start_m, bin_start_ts, bin_end_ts, available_ts, observed(int32), seg_mark(int32)
```
推导：`bin_end = t_ref+T_cum`、`bin_start = bin_end−T_diff`（组件末行 T_cum）；
`spatial_start_m` = 全量 per-(link,sample) 前缀和 `Σ(bin_size×ratio)`（先按 bin_idx 合并组件行再算，§5-④）；
`available_ts` = 下游最近 `observed=1` 行的 bin_end（因果下界，不含管道延迟）。
现产物：`data/windows_adapt_0909/events_2partv4/`（events/ 8 part + adapt_meta.json.d/），
audit：seg1_rows 24,217,588 → dropped_quality 99,144（0.41%）+ dropped_tail_no_fix 1,403,127（5.8%）→ **22,715,317 events**；
closure_mm [min −5.897m, p1 −5mm, p99 +5mm]；meta 记 builder_sha256 与 availability 语义。

### 9.4 windows corpus（`tools/build_windows_spark.py`，主线终点）
`<out>/{window_curves/, window_members/, snapshots/, window_meta.json.d/}`；
snapshot = (map_version, target_link_id, anchor_ts)，回看 W=600s/步进 60s，
bin 准入三条件 `bin_start ≥ t−W`、`bin_end < t`、`available ≤ t`（严格 [t−W,t)）。
读取器拒绝无 meta 的目录；curves 按 snapshot 哈希分片。
运行说明详见 `md/窗口管线使用.md`（独立文档，含训练/导出/边界声明）。

---

## 10. 怎么读数据（三档，服务器执行）

- **本地纯 python**：见 §8 的 pyarrow 片段（footer/schema/小切片，别全量 to_pandas——单 part 3,400 万行会 OOM）。
- **本地 Spark**：qwen12 环境 `QWEN12_PY=/nfs/dataset-ofs-494-1/project/user/junao/ruiqian/qwen12/bin/python`（pyspark 3.5.9 + Java 8）：
  ```bash
  env -u SPARK_HOME SPARK_LOCAL_DIRS=/nfs/dataset-ofs-494-1/project/user/junao/sparktmp \
    PYSPARK_PYTHON=$QWEN12_PY $QWEN12_PY tools/<脚本>.py --inputs 'data/raw_hdfs/event_hour=2026082007/part-*.parquet' ...
  ```
  不带 `env -u SPARK_HOME` 会连上系统 Spark 3.2.0 jar 报 `JavaPackage is not callable`；`SPARK_LOCAL_DIRS` 不给会 spill 到 /tmp（仅 3.7G）撑爆。
- **yarn 全量**：`MODE=dry|yarn bash scripts/submit_*.sh` 三档套路；`HADOOP_USER_NAME=bigdata-dp`、队列 `root.xinsi_yanfaerzu_default`、
  executor 环境 minipy3.tgz（无 pandas/numpy）、HDFS 用全路径。样板：`scripts/submit_ingest_yarn.sh`（资源三档最全）。
- 大数据别 local 档硬跑：22.7M 事件的窗口展开在 local[4] 上 16min+ 未完成；全量直接 yarn。

---

## 11. 坑清单（每条都真实踩过）

1. **minipy3 无 pandas/numpy**：executor 端禁止 mapInPandas / python UDF，纯 SQL。
2. **`--sample-fraction` 是行级**：会拆散 sample（丢一半 bin），sample 级限量必须用 `--max-trajs-per-link`。
3. **本地副本无 `event_hour` 列**（§8），日期从 `t_enter` 推。
4. **`bin_pt_ts` 无 fix 是字符串 `'-1'`**，判空别用 isNull/isNotBlank 想当然。
5. **`isNotNull` 拦不住 NaN/inf**：算术溢出产 NaN（实测 1,727 行逃逸），要显式 `isnan + abs < inf`。
6. **精度**：`ratio` 是 float32，SQL 端 double 计算后与 f32 参照比对要先对齐精度（假差异 ~6e-5）；
   pandas 的 `df.sub` 是减法方法，取列必须 `df['sub']`。
7. **多组件拆 bin**（§5-④）：同 (sample, bin_idx) 多行，唯一化前别建 bin 键。
8. **sub_idx 不能当空间位置**（§5-③）。
9. **curves 分区必须 sample_id hash**；**edges 必须单语料一次算**（多天作业拼接桶不一致）。
10. **pandas 全量必死**：30GB memcg，3.79 亿行 groupBy 用 Spark local（实测 156s）或 yarn；`collect()`/`toPandas()` 照样炸 driver。
11. **NFS 上 spill 慢**：本地 Spark 瓶颈在网络盘溢写不在 CPU，上 yarn 直读 hdfs 反超。
12. **管道里 `grep | tail` 吞退出码**：smoke 脚本要 `set -o pipefail`。
13. **输出目录必须新目录**；无 meta 的中间目录读取器直接拒（防把半成品当完整语料）。

---

## 12. 硬数字锚点速查（自查/验收对照）

| 锚点 | 值 |
|---|---|
| HDFS samples 总量 | 168 分区 × 15 part，841.1G 压缩 / 2.5T 原始 |
| 单 part 行数（2026082007 p0） | 34,827,588 |
| 7 天官方 | 3,354.7 万车次 / 11.42 亿经过；distinct traj 753,395 / link 1,808,031 |
| 静态表 | 2,976,427 link；中位 38m；>200m 仅 5.8% |
| seg1 闭合 | 中位 −1mm（v4 全量 −5mm），p1~p99 ±5mm |
| events_2partv4 | 22,715,317 行（两 part 输入，audit 全记录在 adapt_meta.json.d） |
| data.txt 标本 | L=1455m/166bin/44fix/y=119.73s，§3 四条闭环等式 |

> 数字来源：9.7–9.9 进度 md（`md/9.7|9.8|9.9progress.md`）+ 本次 `hdfs dfs -ls/du` 实测。
> 数据语义有新发现时**更新本文档**，别只写进进度 md。
